import { Container } from "@mui/material";
import React from "react";

type Props = {};

export default function PartnerDetails({}: Props) {
  return <Container maxWidth="sm">select compamny</Container>;
}
